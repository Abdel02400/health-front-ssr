import type { ReactElement } from 'react';

function AuthenticatedActions(): ReactElement {
    return (
        <div className='authenticated-actions'>

        </div>
    );
}

export default AuthenticatedActions;