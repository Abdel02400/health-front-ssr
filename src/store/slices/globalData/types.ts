import type { ThemeType } from '@shared-types/theme';

export type GlobalDataStateType = {
    theme: ThemeType;
};

export type { ThemeType } from '@shared-types/theme';
export { THEME, THEME_STORAGE_KEY } from '@shared-types/theme';